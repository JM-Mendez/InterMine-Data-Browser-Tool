import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks'
import { withKnobs, text, select, boolean } from '@storybook/addon-knobs'
import { fontSizes } from '../../src/theme'
import { allColors } from '../../src/theme/colorPalette'
import * as Icons from '../../src/images/icons'

import { Button } from '../../src/components/Button'

<Meta title="Components/Button" component={Button} />

<Preview>
	<Story name="Button" parameters={{ decorators: [withKnobs] }}>
		<Button
			intent={select('intent', ['primary', 'info', 'danger'])}
			fontSize={select('fontSize', Object.keys(fontSizes.desktop), 's2')}
			fontColor={select('fontColor', allColors, allColors.blue000)}
			iconColor={select('iconColor', allColors, allColors.blue000)}
			icon={select(
				'icon',
				['none', ...Object.keys(Icons).map((i) => Icons[i].displayName)],
				undefined
			)}
			iconRightColor={select('iconRightColor', allColors, allColors.blue000)}
			iconRight={select(
				'iconRight',
				['none', ...Object.keys(Icons).map((i) => Icons[i].displayName)],
				undefined
			)}
		>
			{text('Button Text', 'Run Query')}
		</Button>
	</Story>
</Preview>

<Props of={Button} />
